//Quando criamos uma variavel e não atribuimos um valor, no terminal imprime "undefined".
let cor;

// Atualizando o valor da variável.
cor = "azul";

//Imprimindo o valor da variável no terminal.
console.log(cor);

//Tipo de dados boolean (true ou false).

let EhEstrangeiro = false;

console.log(EhEstrangeiro);

let listaDeTamanho = ['P', 'M', 'G', 52, 54];
console.log(listaDeTamanho);

listaDeTamanho.push(false);
 console.log(listaDeTamanho);

 //Atributos e funções tipos de dados.

 console.log(cor.length)